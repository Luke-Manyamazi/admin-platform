{
  "name": "admin-platform",
  "version": "0.0.1",
  "private": true,
  "description": "ADMIN — African Distributed Manufacturing & Industrial Network by Camluk Technologies",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,json,md,yaml,mjs}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md,yaml,mjs}\" --ignore-path .gitignore",
    "db:generate": "turbo run db:generate --filter=@admin-platform/database",
    "db:migrate": "turbo run db:migrate --filter=@admin-platform/database",
    "db:migrate:deploy": "turbo run db:migrate:deploy --filter=@admin-platform/database",
    "db:studio": "cd packages/database && pnpm exec prisma studio",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.0",
    "tsx": "^4.19.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.20.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@11.3.0"
}

{
  "name": "@admin-platform/web",
  "version": "0.0.1",
  "private": true,
  "description": "ADMIN Connect — Buyer portal (Next.js 14 App Router)",
  "scripts": {
    "build": "next build",
    "dev": "next dev --port 3000",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "start": "next start --port 3000",
    "clean": "rimraf .next out"
  },
  "devDependencies": {
    "@admin-platform/config": "workspace:*"
  }
}

{
  "name": "@admin-platform/events",
  "version": "0.0.1",
  "private": true,
  "description": "ADMIN Events — Typed AWS EventBridge event definitions with source, detailType, and detail shape",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  }
}

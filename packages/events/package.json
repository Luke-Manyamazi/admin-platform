{
  "name": "@admin-platform/events",
  "version": "0.0.1",
  "private": true,
  "description": "ADMIN Events — Typed AWS EventBridge event definitions",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@admin-platform/types": "workspace:*"
  },
  "devDependencies": {
    "@admin-platform/config": "workspace:*"
  }
}
